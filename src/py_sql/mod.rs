#[derive(Clone, Debug)]
pub enum NodeType {
    Null,
    // NString(StringNode),
    // NIf(IfNode),
    // NTrim(TrimNode),
    // NForEach(ForEachNode),
    // NChoose(ChooseNode),
    // NOtherwise(OtherwiseNode),
    // NWhen(WhenNode),
    // NBind(BindNode),
    // NSet(SetNode),
    // NWhere(WhereNode),
    // NPrint(PrintNode),
}

